{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5436;Username=postgres;Password=basket_password;Database=basket_db;",
    "CapConnection": "Host=localhost;Port=5437;Username=postgres;Password=cap_password;Database=cap_db"
  },
  "ProductApi": {
    "GraphQLEndpoint": "http://localhost:50055/graphql/product"
  },
  "Kafka": {
    "Servers": "localhost:9092",
    "Username": "kafka_user",
    "Password": "kafka_password"
  }
}
